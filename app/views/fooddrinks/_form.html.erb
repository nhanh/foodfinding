<%= form_for(@fooddrink) do |f| %>
  <% if @fooddrink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fooddrink.errors.count, "error") %> prohibited this fooddrink from being saved:</h2>

      <ul>
      <% @fooddrink.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fdName %><br />
    <%= f.text_field :fdName %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.select :category_id, options_for_select(Category.all.collect{|c| [c.cateName, c.id]}) %>
  </div>
  <div class="field">
    <%= f.label :restaurant_id %><br />
    <%= f.select :restaurant_id, options_for_select(Restaurant.all.collect{|r| [r.restName, r.id]}) %>
  </div>
  <div class="field">
    <%= f.label :Price %><br />
    <%= f.number_field :Price %>
  </div>
  <div class="field">
    <%= f.label :rateNumber %><br />
    <%= f.text_field :rateNumber %>
  </div>
  <div class="field">
    <%= f.label :fdImage %><br />
    <%= f.text_area :fdImage %>
  </div>
  <div class="field">
    <%= f.label :dateUpload %><br />
    <%= f.date_select :dateUpload %>
  </div>
  <div class="field">
    <%= f.label :Type %><br />
    <%= f.select :Type, options_for_select([["Thuc An", "0"], ["Thuc Uong", "1"]]) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
